{
  "name": "projectflow",
  "version": "1.0.0",
  "description": "Project Management Tool",
  "main": "dist/app.js",
  "scripts": {
    "dev": "npm run build && npx serve -s . -l 3000",
    "start": "npm run build && npx serve -s . -l 3000",
    "build": "npx babel app.js --out-file dist/app.js --presets=@babel/preset-react,@babel/preset-typescript"
  },
  "keywords": ["project-management", "tasks", "calendar"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "lucide-react": "^0.263.1"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "@babel/cli": "^7.22.0",
    "@babel/core": "^7.22.0",
    "@babel/preset-react": "^7.22.0",
    "@babel/preset-typescript": "^7.22.0"
  }
}